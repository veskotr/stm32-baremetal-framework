# Shared project settings. Personal model and approval preferences belong in
# ~/.codex/config.toml. Project settings load only when this repository is trusted.

[agents]
max_threads = 4
max_depth = 1
interrupt_message = true
