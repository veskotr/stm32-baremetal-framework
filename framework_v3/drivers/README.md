# Drivers

Reusable driver integrations will live here.

This folder currently exposes the placeholder `hss_drivers` CMake target so applications can link the framework through one stable surface while driver integrations are added incrementally.

Planned initial driver integration:

- Analog Devices no-OS STM32 support

